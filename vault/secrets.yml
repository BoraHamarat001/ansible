---
# ---------------------------------------------------------------------------
# HASSAS VERİLER - Bu dosyayı şifreleyin!
#
# Şifrelemek için:
#   ansible-vault encrypt vault/secrets.yml
#
# Düzenlemek için:
#   ansible-vault edit vault/secrets.yml
#
# Playbook çalıştırırken:
#   ansible-playbook db.yml --ask-vault-pass
#   ya da:
#   ansible-playbook db.yml --vault-password-file ~/.vault_pass
# ---------------------------------------------------------------------------

mysql_root_password: "DEĞİŞTİRİN"
mysql_password: "DEĞİŞTİRİN"
db_password: "DEĞİŞTİRİN"
